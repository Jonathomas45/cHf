BRANCHES

Main:               is code that was written when first messing around the data set for recipes.csv was used to form views of the data a recipe at a time. This was also used to get a list of recipe names
                    DATACOMM- is the exploration of SQL communication to talk with a Mysql server and keeping that as the main source to draw data from for quaries on the recipes.

DataCOMM update:    This is an improved form of the main code for DATACOMM include some working examples of that data
====================================================================================================================================================================================================================
EXAMPLES OF CURRENT PROGRESS

main.py is showing the main components of RecipeBook.py
test.py is showing the fuctionality of DATACOMM.py
=====================================================================================================================================================================================================================
FOLDERS

/progress will have a screenshot of how the data was displayed 
/datasets this hold the recipes.csv and the list of meal_names
/scripts shows has the files with the working code that is demonstrated on the above two .py files
